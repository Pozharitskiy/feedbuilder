# FeedBuilderly - Environment Variables with Billing Enabled
# Copy this to .env and fill in your values

# Shopify App Credentials (from Partners Dashboard)
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here

# App URL (your Fly.io or production URL)
APP_URL=https://feedbuilder.fly.dev
HOST=feedbuilder.fly.dev

# Scopes - UPDATED FOR BILLING! 
# Required: read_products, write_products for feeds
# Required: read_subscriptions, write_subscriptions for billing
SCOPES=read_products,write_products,read_subscriptions,write_subscriptions

# Optional: Add these for enhanced features
# SCOPES=read_products,write_products,read_subscriptions,write_subscriptions,read_orders,read_customers

# Node Environment
# - "development" = TEST mode billing (no real charges)
# - "production" = LIVE mode billing (real charges!)
NODE_ENV=development

# Port (default 8080 for Fly.io)
PORT=8080

# Database path (auto-created)
DB_PATH=./feedbuilder.db

# Optional: Monitoring & Analytics
# SENTRY_DSN=your_sentry_dsn
# ANALYTICS_ID=your_analytics_id

# Optional: Email notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ⚠️ IMPORTANT NOTES:
# 1. After changing SCOPES, you MUST re-install the app on all stores
# 2. Use NODE_ENV=development for testing (no real charges)
# 3. Switch to NODE_ENV=production only after thorough testing
# 4. Keep API_SECRET secure - never commit to git

